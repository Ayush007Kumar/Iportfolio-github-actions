#To deploy the index.html pages to GitHub Pages (service by github that runs static website)
name: Deploy Static Site
#when does this workflow runs 
on:
  push:
    branches: [main]
    #you control and deploy by a button 
  workflow_dispatch:  
#required permission for the workflow 
permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5

      - name: Setup GitHub Pages Configuration
        id: pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload Artifacts
        uses: actions/upload-pages-artifact@v5
        with:
          path: '.' # Location of your static files

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
